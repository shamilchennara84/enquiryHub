import express from "express";
// import { validateSearchQuery } from "../middlewares/validationMiddleware"; 
// import typesenseController from "../controllers/typeSenseController"; 
const typesenseRouter = express.Router();

// // Typesense search routes
// typesenseRouter.get("/search/userProfiles", validateSearchQuery, typesenseController.searchUserProfiles);
// typesenseRouter.get("/search/enquiries", validateSearchQuery, typesenseController.searchEnquiries);



export default typesenseRouter;